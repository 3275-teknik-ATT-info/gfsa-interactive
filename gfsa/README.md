# GFSA

Canonical directory layout for stage-bound elevation.

Elevation is a pure function of current stage (`contract/execution.yaml`).

## Quick start
```bash
make scaffold-diff
make bootstrap
make validate
